name: bazel (vm bento) x64

on:
  workflow_dispatch:

jobs:
  build:
    runs-on:
    - self-hosted
    - "os=linux"
    - "arch=x64"
    - "engflow-cluster=local"
    - "engflow-bento-allow-save=true"
    - "engflow-bento-vm-base-image=ubuntu-24.04"
    - "engflow-bento-branch=main"

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 1

    - name: Run bazel
      run: |
        set && which bazel && bazel info
